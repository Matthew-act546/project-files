
def main():
    print("What is you're pin?")
    pin = int(input())

    up_to = 1
    while up_to <= 999999:
        print(up_to)
        
        if up_to == pin:
            break
        up_to += 1
    sum(pin)
    """
    for up_to in range(999999):
        print(up_to)
        
        if up
        collector = 0
        if up_to == pin:

            break
        else:
            print()"""

main()